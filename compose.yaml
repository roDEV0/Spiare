name: "spiare"

services:
  watcher:
    environment:
      DATABASE_URL: ${DATABASE_URL}
    extra_hosts:
      - database:${DATABASE_HOST}
    build:
      context: .
      dockerfile: watcher/Dockerfile