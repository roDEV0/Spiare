name: "spiare"

services:
  watcher:
    environment:
      DATABASE_URL: ${DATABASE_URL}
    build:
      context: .
      dockerfile: watcher/Dockerfile
      extra_hosts:
        database: ${DATABASE_HOST}